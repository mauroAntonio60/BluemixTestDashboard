<server>

    <featureManager>
        <feature>webProfile-7.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

	<webContainer deferServletLoad="false"/>

    <application name="TestDashboard" context-root="/" location="${appLocation}" type="war"/>

</server>
